[package]
name = "aev-edge-checkin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spin-sdk = { version = "2", features = ["http", "key-value", "wasi-experimental-http"]}
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.16"
